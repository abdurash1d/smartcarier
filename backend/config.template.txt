# =============================================================================
# SmartCareer AI - Backend Configuration Template
# =============================================================================
# Bu faylni .env ga nusxa oling va qiymatlarni to'ldiring
# copy config.template.txt .env

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
APP_NAME="SmartCareer AI"
APP_VERSION="1.0.0"
DEBUG=true
SECRET_KEY=your-super-secret-key-change-this-in-production

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# Development uchun SQLite:
DATABASE_URL=sqlite:///./smartcareer.db

# Production uchun PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/smartcareer

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION
# -----------------------------------------------------------------------------
JWT_SECRET_KEY=your-jwt-secret-key-change-this
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# 🌟 GOOGLE GEMINI (BEPUL! - Tavsiya etiladi)
# -----------------------------------------------------------------------------
# 1. https://ai.google.dev/ ga boring
# 2. "Get API key" bosing
# 3. Google hisobingiz bilan kiring
# 4. Yangi kalit yarating
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash

# AI provider tanlash: "gemini" (bepul) yoki "openai" (pullik)
AI_PROVIDER=gemini

# -----------------------------------------------------------------------------
# OPENAI (Ixtiyoriy - pullik)
# -----------------------------------------------------------------------------
# 1. https://platform.openai.com ga boring
# 2. API Keys bo'limiga kiring
# 3. Create new secret key
# 4. Shu yerga yozing:
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.7
OPENAI_TIMEOUT=60
OPENAI_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# 📧 EMAIL (SMTP)
# -----------------------------------------------------------------------------
# Gmail uchun:
# 1. Google Account > Security > 2-Step Verification ON
# 2. App passwords > Generate
# 3. Shu parolni SMTP_PASSWORD ga yozing
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@smartcareer.uz
SMTP_FROM_NAME=SmartCareer AI
SMTP_USE_TLS=true

# SendGrid (Production uchun tavsiya)
# https://sendgrid.com dan ro'yxatdan o'ting
SENDGRID_API_KEY=

# Frontend URL (email havolalari uchun)
FRONTEND_URL=http://localhost:3000

# Support email
SUPPORT_EMAIL=support@smartcareer.uz

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


 
